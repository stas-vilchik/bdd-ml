{
  if (this.props.x === 459) {
    return React.createElement("div", {
      style: {
        position: "absolute",
        pointerEvents: "auto",
        transform: "translate3d(0px,65px,0)",
        backfaceVisibility: "hidden"
      }
    });
  }
}
